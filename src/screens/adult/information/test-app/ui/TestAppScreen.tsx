import { PrimaryContainer } from '@/shared/ui';
import { TestApp } from '@/widgets/adult';

const TestAppScreen: React.FC = () => {
  return (
    <PrimaryContainer>
      <TestApp />
    </PrimaryContainer>
  );
};

export default TestAppScreen;
